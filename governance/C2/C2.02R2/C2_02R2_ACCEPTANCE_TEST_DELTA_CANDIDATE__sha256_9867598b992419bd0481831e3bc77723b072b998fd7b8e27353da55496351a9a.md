# C2.02R2 — Narrow Repair Acceptance Test Delta Candidate

Generated: 2026-08-24T09:07:53Z

Status: `NARROW_REPAIR_CANDIDATE_PENDING_INDEPENDENT_RECHECK`

Range: `C2-168..C2-180` (13 narrow-repair obligations)

These obligations supplement, not rewrite, prior C2.02/C2.02R1 acceptance authority. The independent re-check must also re-evaluate residual failed R1 IDs `C2-145` and `C2-156` against the R2 successor semantics.

## C2-168 — Cancellation-origin domain is closed and ordered
The cancellation-origin enum has one frozen canonical ordinal covering CALLER, ORCHESTRATOR, DEADLINE_CONTROLLER, SHUTDOWN, SUPERSEDED_REQUEST, and UNKNOWN; private extension values are forbidden.

Decision refs: `C2.DEC-122`

## C2-169 — Same-phase multiple cancellations have one primary origin
Given multiple established EXPLICIT_CANCELLATION origins at the same phase, the primary `cancellation_origin` is selected by the frozen ordinal over known origins, using UNKNOWN only when no known origin exists.

Decision refs: `C2.DEC-123`

## C2-170 — Secondary cancellation origins are losslessly retained
All non-primary established same-phase cancellation origins are retained uniquely in `secondary_cancellation_origins[]`, excluding the primary and using canonical ordinal order.

Decision refs: `C2.DEC-123`

## C2-171 — Same pinned cancellation candidate set is replay deterministic
Two implementations receiving the same pinned same-phase explicit-cancellation candidate set produce the same CANCELLED status, primary origin, and secondary-origin list.

Decision refs: `C2.DEC-124`

## C2-172 — Incomplete cancellation-origin candidate set fails closed
If the complete established same-phase explicit-cancellation origin set cannot be reconstructed deterministically, the adapter does not race-select an origin and fails `ADAPTER_INVARIANT_VIOLATION`.

Decision refs: `C2.DEC-124`

## C2-173 — Attempt requirement is replay-retained
The exact normalized C2.01R1 `CapabilityRequirement R` used to establish compatibility for the concrete attempt is the load-bearing normalized request boundary, is replay-retained under the same registry snapshot, and provider dispatch is forbidden if the pinned request semantic state cannot yield one unique canonical R.

Decision refs: `C2.DEC-125`

## C2-174 — Attempted response scope has an exact set equation
`attempted_response_semantic_refs[]` equals exactly the canonical set union of endpoint primary response semantic, `P.required_response_semantic_refs[]`, and `R.required_response_semantic_refs[]`.

Decision refs: `C2.DEC-126`

## C2-175 — Attempted response scope uses exact stable-ref identity
Every attempted-scope member is an allowed C2.01R1 response-closure kind, exact-resolves under S, deduplicates by full stable-ref identity, and serializes in deterministic `(ref_kind, logical_id, semantic_revision, content_sha256)` order.

Decision refs: `C2.DEC-126`

## C2-176 — Query-dependent response effects are materialized before dispatch
Any explicit request/query condition that changes required response semantics is represented in `R.required_response_semantic_refs[]` before dispatch; C2.02 does not guess the effect by recursively dereferencing query semantics.

Decision refs: `C2.DEC-127`

## C2-177 — Unresolved attempted response scope fails before dispatch
If query-dependent required response semantics cannot be deterministically materialized, the operation fails at PRE_DISPATCH with `RESPONSE_SEMANTIC_INVALID / ATTEMPT_RESPONSE_SCOPE_UNRESOLVED`, zero admitted observations, and no NO_DATA inference.

Decision refs: `C2.DEC-127`

## C2-178 — Broader C2.01 response closure is not silently imported
`attempted_response_semantic_refs[]` does not automatically import endpoint-supported grouping/field/pagination/provider-signal refs from the broader C2.01R1 compatibility closure unless explicitly required by P or R.

Decision refs: `C2.DEC-128`

## C2-179 — Identical pinned attempt state yields identical attempted scope
For identical `(S, provider_capability_ref, endpoint_semantic_profile_ref, R)`, all conforming implementations derive byte-equivalent canonical `attempted_response_semantic_refs[]` and therefore feed the same scope into partiality materiality.

Decision refs: `C2.DEC-128`

## C2-180 — R2 repair boundary remains narrow
R2 introduces no routing/fallback, retry policy, credentials, completeness methodology, C1 wire mapping, production provider selection, production adapter, or PAPER/LIVE side-effect authority.

Decision refs: `C2.DEC-129`
