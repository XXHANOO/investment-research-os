# C2.03R1 — Scoped Repair Diffs

Generated: 2026-08-24T11:34:41Z

Parent C2.03 review result: `d1a46f949f42612b963b742107ef9fd819b257821b5e4bbfa0d422d3f62618ba`

This document is descriptive only; the successor contract/model are the repair candidate.

## C2ROUTE-B01

Added C2.03-owned full-tuple `C2RoutingStableRef`, `CandidateSetDefinition`, exact `route_candidate_ref=(candidate_set_ref,id)`, exact policy-to-candidate-set binding, and closed `ALL_IN_CANDIDATE_SET|ALLOWLIST` permission predicate. Content identity is SHA-256 over RFC8785 JCS canonical normative bodies.

## C2ROUTE-B02

Defined `matches(rule,attempt)` exactly: continuation class exact equality; absent qualifiers are wildcards; present qualifiers compare exactly; failure qualifiers are legal only for FAILED; `failure_code` requires `failure_family`; illegal/unknown state fails closed. Tie-break occurs only after exact match-set construction.

## C2ROUTE-B03

Added immutable `ExternalAdmissionBlockRecord`, event-level `triggering_external_admission_block_ref` on external-admission fallback, and `RouteStateKey=(route_candidate_ref,effective_requirement_identity)` consumption. Fallback-consuming blocks prevent reselection loops; YIELD preserves the same pending unconsumed state.

## C2ROUTE-B04

Added explicit `AuthorizedRequirementTransitionInput`, closed action `NOMINATE_CANDIDATE_UNDER_DOWNGRADED_REQUIREMENT`, `RequirementTransitionEvent`, and requirement-keyed route state. `A@R` and `A@R'` are distinct route states; same candidate under R' is legal after revalidation and is not SAME_ROUTE_REATTEMPT.

## Deliberately unchanged ownership

C3 still owns admission/quota/backoff/cache/freshness/coalescing/LKG; C6/C7 still own trusted authorization/orchestration; C2.06 still owns exact cross-contract wire binding; C2.07 still owns machine schema/validator encoding. No provider instance, vendor priority instance, production adapter, credential implementation, or PAPER/LIVE side effect was added.
