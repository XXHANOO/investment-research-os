# C2.05R2 — Scoped Repair Diffs

Generated: 2026-09-07T07:10:00Z

Status: `CONSTRUCTION_DIFF_RECORD_ONLY`

This document records only the semantic delta from effective C2.05 + C2.05R1 to the C2.05R2 repair candidate.

## B03 — operation-scope provenance

### Before R2
R1 compared a self-contained `OperationCoverageScopeBinding` field-by-field to `CoverageScopeDefinition`, but did not machine-bind the duplicated fields back to the actual replay-retained C2.01 `CapabilityRequirement R`, the exact C2.02R2 attempted-response set, or an authoritative pre-dispatch source for native/partition/constraint/temporal dimensions.

### R2 delta
- add `OPERATION_COVERAGE_PROVENANCE`;
- require exactly one coverage provenance ref for a load-bearing operation/attempt;
- bind embedded canonical `CapabilityRequirement R` to the exact replay-retained R;
- bind attempted-response semantics to the exact C2.02R2 stored/recomputed set;
- make native subject / identifier / partition / constraint / temporal fields authoritative only through the exact pre-dispatch provenance commitment;
- require `OperationCoverageScopeBinding` to be an exact deterministic projection of provenance before it can be compared to the target scope;
- forbid post-hoc provenance/binding minting as a coverage-support path.

This directly targets residual failures `C2-437`, `C2-438`, `C2-481`, and `C2-525`.

## B04 — composite root/component relation

### Before R2
R1 allowed root and component query/response semantics to differ solely because component scopes were explicitly enumerated. Component enumeration proved membership, not semantic coverage of an independently asserted root query/response universe.

### R2 delta
C2.05R2 selects the stricter reviewed repair form:

```text
for COMPOSITE_EXPLICIT_CLOSURE:
  root.required_query_semantic_refs    = []
  root.required_response_semantic_refs = []
  root composite query/response universe
    = exact canonical composite_component_scope_refs[]
```

Each component scope retains its own exact query/response semantics.

For partitioned composites:
- exact component-scope closure AND exact partition closure are both mandatory.

For non-partition composites:
- exact component-scope bijection with all-empty partition sets is the gap proof.

A root with non-empty query/response refs is structurally invalid, so there is no conflicting parallel root universe.

This directly targets residual failures `C2-471`, `C2-481`, `C2-539`, and `C2-540`.

## Preserved surfaces

No semantic change is made to:
- B01 coverage-evidence carrier closure;
- B02 certification total decision precedence;
- B05 total coverage classifier;
- C2.04R1 secure ingress;
- C1/C3/C4/C5/C6/C7/C11/C12 ownership;
- concrete provider/vendor configuration;
- production implementation or side-effect authority.
