# C2.04R1 — Scoped Repair Diffs

Generated: 2026-09-07T05:36:00Z

Parent C2.04 contract: `198d194fbb3814676f1cb74aec5d56f19f8b1f9b081c6d77c16b4e66fd762cda`  
Parent independent review: `FAIL` report `12aead5a1d4fc5eea0813cedbf360fd68616b432f34100db341367d96405fe21` / result `4eb3daf2cecb0478731b814dd7749f06e990b5748f9301b8792c84dff9c22f78`.

## Scope

Only `C2SEC-B01..B04` are modified. All other parent C2.04 semantics are inherited unchanged.

## R1-1 — B01 redirect-forwarding closure

Replaces undefined `redirect_forwarding_semantics` with a closed enum and exact ordered predicate. Cross-provider, cross-authority, capability-changing, unbound-endpoint, and unresolved redirects deterministically block. Same-authority exact-endpoint forwarding is permitted only under `FORWARD_SAME_AUTHORITY_SAME_ENDPOINT`; otherwise a permitted same-authority target requires rebind or blocks according to the pinned mode.

## R1-2 — B04 exact lease operation identity

Adds required `provider_operation_ref` and `route_attempt_ref` to `CredentialLease`, exact-match use semantics, `LEASE_OPERATION_IDENTITY_MISMATCH`, no cross-operation/attempt lease reuse, and fresh lease IDs for rebind/new attempts.

## R1-3 — B02 private-state scope machine closure

Adds stable `PRIVATE_STATE_SCOPE` atoms and exact canonical set semantics: exact resolution, conflicting-revision rejection, duplicate removal, deterministic sorting, and exact-set subset evaluation. Adds operation-local `actual_private_state_scope_refs[]`. Replaces free-form `public_projection_rule` with exact `public_projection_rule_ref` to a stable `PUBLIC_PROJECTION_RULE` carrying a closed projection mode and metadata allowlist.

## R1-4 — B03 secure-ingress assessment-bundle integrity

Defines `SecureIngressAssessmentRecord` and `SecureIngressAxisResult`, exactly one SECRET/PRIVACY/LICENSING/RETENTION result, one candidate, one provider operation, one exact ingress profile, exact corresponding pinned policy refs, deterministic invalid-bundle quarantine, exact decision recomputation/linkage, and fresh full assessment after redaction.

## R1-5 — acceptance and traceability

- re-checks failed parent obligations `C2-313` and `C2-319`;
- adds `C2-349..C2-401` (53) adversarial obligations;
- adds `C2.DEC-263..C2.DEC-287` (25) repair decisions;
- all four blockers remain `ADDRESSED_NOT_CLOSED` pending independent re-check;
- no C3/C4/C5/C6/C7/C11 ownership is absorbed;
- C2.05, production implementation, and external/PAPER/LIVE side effects remain unauthorized.
